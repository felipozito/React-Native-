import { View, Text } from "react-native";
import React from "react";
import Button from "../Components/Button";

const Home = () => {
      return (
            <View>
                  <Text>Home</Text>
                  <Button nombre={"eliminar"} />
            </View>
      );
};

export default Home;
